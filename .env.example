# el-linear — Enrich Layer Linear CLI
# Token can also be stored in ~/.linear_api_token

# Linear personal API token
# LINEAR_API_TOKEN=lin_api_...

# Set to any value to enable debug logging (stderr)
# EL_LINEAR_DEBUG=1
