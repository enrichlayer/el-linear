node_modules/
dist/
.env
.env.*
!.env.example
.env.local
.env.production
*.pem
*.key
credentials.json
.npmrc
coverage/
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
*.bak
*.orig
*.swp
*.swo
.cache/
tmp/
.tmp/
.product-finalizer-skip.json

# npm pack release artifacts (`pnpm pack` / `npm pack` output)
*.tgz

# Playwright MCP traces — local artifacts only
.playwright-mcp/

# Claude Code per-user runtime + local settings.
# If you later want to commit team-shared skills/agents/hooks, un-ignore
# specific subpaths (e.g. `!.claude/skills/`) below this line.
.claude/

# Agent-tooling runtime (agents-spec parallel of `.claude/`). Some setups fill
# `.agents/skills/` with symlinks into a *separate* checkout, which is why the
# scanners exclude it — see scan-excludes.test.ts.
.agents/
